# API Trabalho Final LP3

### Instruções

- Acesse o diretório do projeto 
- Dentro do diretório, digite os comandos:

```./gradlew clean build```

```./gradlew build```  

- Rode a aplicação com o comando:

```./gradlew bootRun```

